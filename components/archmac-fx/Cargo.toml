[package]
name = "archmac-fx"
version = "0.1.0"
edition = "2024"
description = "ARCHMAC Galaxy terminal motion and effects engine"
license = "MIT"

[dependencies]
ratatui = "0.30.2"
tachyonfx = "0.25.1"

[profile.release]
strip = true
lto = "thin"
codegen-units = 1
panic = "abort"
